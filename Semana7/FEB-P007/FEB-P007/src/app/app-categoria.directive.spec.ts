import { AppCategoriaDirective } from './app-categoria.directive';

describe('AppCategoriaDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCategoriaDirective();
    expect(directive).toBeTruthy();
  });
});
